Em Java, a palavra-chave static indica que um membro (atributo ou método) pertence à própria classe, e não a uma instância (objeto) específica dela.


Uma variável comum (de instância) ganha um valor novo para cada objeto criado. Já uma variável static é única e compartilhada.